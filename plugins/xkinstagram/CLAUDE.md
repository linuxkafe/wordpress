# xkinstagram Plugin

## Intent
A WordPress Instagram plugin project.

## Non-Goals
- Do not over-engineer initial setup
- No heavy external dependencies initially

## Critical Files
- `Makefile` — build/lint/test commands
- `aes/` — AES project tracking
- `composer.json` — WordPress plugin dependencies

## Never-do
- Do not commit .env files
- Do not skip lint checks

## Evidence Required
- Tests must pass before commit
- Lint must pass before commit
