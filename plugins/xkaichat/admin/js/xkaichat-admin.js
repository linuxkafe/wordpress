<?php
/**
 * Marcador para o JS admin existir no bundle.
 * (O JS usa strings i18n localizadas — placeholder vazio mantém o enqueue válido.)
 *
 * @package XKaiChat
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}